app:
  scheduler:
    enable: true
    interval: 5000
    force-check-delay: 10s

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

api:
  baseurl:
    stackoverflow: https://api.stackexchange.com/2.3/questions/
    github: https://api.github.com/networks/
